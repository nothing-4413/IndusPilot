# Tasks

## 1. OpenSpec

- [x] 1.1 定义 CI 非默认数据库名称契约。

## 2. Implementation

- [x] 2.1 在两条 CI 依赖环境链路设置相同的自定义 MySQL 数据库名。
- [x] 2.2 在 dependency smoke 和运行时 HTTP profile 中断言/传递数据库名。
- [x] 2.3 扩展部署预检和文档说明该 CI 覆盖。

## 3. Verification and delivery

- [x] 3.1 运行脚本语法、预检、质量和差异检查。
- [ ] 3.2 提交、推送并归档本变更。
