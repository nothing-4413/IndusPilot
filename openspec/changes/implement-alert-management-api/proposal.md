# Change: 实现告警管理 HTTP API

## Why

运行监控已经能够写入设备运行态，但告警管理仍只有列表入口。为了形成资产、运行态、告警、工单的业务链路，需要提供告警创建、查询和生命周期流转 API。

## What Changes

- 扩展告警服务，支持按资产、严重度和状态筛选。
- 新增告警创建、详情、确认、分派、解决和关闭 HTTP API。
- 对告警严重度和状态输入进行显式校验。
- 扩展集成测试和开发文档。

## Non-Goals

- 不实现自动告警规则引擎。
- 不实现短信、邮件或 WebSocket 推送。
- 不接入真实时序数据库或消息队列。