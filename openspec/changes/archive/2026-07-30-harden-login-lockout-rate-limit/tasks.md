## 1. 登录失败锁定

- [x] 1.1 增加登录失败窗口、阈值和锁定时长配置。
- [x] 1.2 身份服务记录连续失败并在锁定期拒绝登录。
- [x] 1.3 HTTP 登录接口返回 `429` 与 `Retry-After`。
- [x] 1.4 登录失败和锁定写入操作审计。

## 2. 验证

- [x] 2.1 C++ 单测覆盖锁定策略。
- [x] 2.2 HTTP smoke 覆盖锁定响应。
- [x] 2.3 更新示例配置并运行 OpenSpec、CTest、CI。