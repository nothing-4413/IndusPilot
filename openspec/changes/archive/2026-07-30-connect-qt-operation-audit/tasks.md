## 1. OpenSpec

- [x] 1.1 编写操作审计闭环变更说明和规格增量。

## 2. 后端与持久化

- [x] 2.1 新增操作审计领域模型、服务和仓储接口。
- [x] 2.2 新增内存仓储和 MySQL 仓储实现。
- [x] 2.3 新增 MySQL 007 schema 并纳入部署前预检。

## 3. HTTP 与权限

- [x] 3.1 新增 `audit:read` 权限并仅授予管理员默认角色。
- [x] 3.2 新增 `GET /api/v1/audit/events` 查询接口。
- [x] 3.3 记录登录、告警通知派发和重试操作审计事件。

## 4. Qt 客户端

- [x] 4.1 新增操作审计 API 客户端方法和离线兜底数据。
- [x] 4.2 新增“操作审计”导航页和表格刷新。

## 5. 验证与提交

- [x] 5.1 补充基础测试和 HTTP 集成冒烟测试。
- [x] 5.2 运行 OpenSpec、后端、HTTP 和 Qt 构建测试。
- [x] 5.3 提交并推送 GitHub，确认 GitHub Actions 结果。