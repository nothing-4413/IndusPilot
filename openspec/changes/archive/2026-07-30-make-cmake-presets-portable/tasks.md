## 1. Presets

- [x] 1.1 将 vcpkg toolchain 路径改为 `VCPKG_ROOT`。
- [x] 1.2 将 Redis/HTTP 测试运行时 PATH 改为 `VCPKG_ROOT`。

## 2. 文档与预检

- [x] 2.1 更新本地工具链文档。
- [x] 2.2 在部署预检中阻断 CMakePresets 本机 vcpkg 路径硬编码。
- [x] 2.3 运行 OpenSpec、CMake/CTest、提交并推送 GitHub。