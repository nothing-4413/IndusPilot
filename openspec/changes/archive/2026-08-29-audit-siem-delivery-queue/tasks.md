# Tasks

- [x] 1. 增加 SIEM 投递任务领域类型、仓储接口及内存/MySQL 实现。
- [x] 2. 新增可幂等 MySQL 迁移，创建 SIEM 任务表和到期任务索引。
- [x] 3. 将 AuditService 接入受控队列 worker、租约、退避重试和死信状态。
- [x] 4. 让 SIEM sender 返回可判定结果，并将配置和 HTTP context 连接到队列服务。
- [x] 5. 增加 SIEM 投递指标与配置校验，并更新示例和部署预检。
- [x] 6. 扩展单元测试，覆盖重试、死信、停止和指标；构建并运行完整测试集。
